<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/action_add"
        android:orderInCategory="100"
        android:title="Add"
        android:icon="@mipmap/ic_menu_add"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_delete"
        android:orderInCategory="100"
        android:title="Delete"
        android:icon="@mipmap/ic_menu_delete"
        app:showAsAction="ifRoom"/>

    <item android:title="List"
        android:id="@+id/action_listview"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:title="GridView"
        android:id="@+id/action_gridview"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:title="HorizontalGridView"
        android:id="@+id/action_hgridview"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:title="StaggeredGridView"
        android:id="@+id/action_staggered"
        android:orderInCategory="100"
        app:showAsAction="never" />
</menu>